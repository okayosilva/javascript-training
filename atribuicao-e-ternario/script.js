let scroll = 1000;
scroll += 500;

console.log(scroll);

let ownAcar = true,
  ownHouse = true;

let giveCredit =
  ownAcar && ownHouse ? "Pode dar crédito" : "Não pode dar crédito";

console.log(giveCredit);
